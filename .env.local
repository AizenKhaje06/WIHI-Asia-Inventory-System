# .env.local - Local Environment Variables for Inventory Pro
# IMPORTANT: 
# 1. Replace the placeholders below with your actual Google Sheets credentials.
# 2. Do NOT commit this file to Git (add to .gitignore if not already).
# 3. For GOOGLE_PRIVATE_KEY, copy the entire key from your service account JSON.
#    If it has multiple lines, replace actual newlines with \n (e.g., "-----BEGIN PRIVATE KEY-----\nMII...").
# 4. After filling in, save and run 'pnpm dev' to test locally.
# 5. For production (Vercel), add the same vars in Vercel dashboard > Settings > Environment Variables.

# Google Sheets Integration (Required)
GOOGLE_SHEET_ID=your_google_sheet_id_here  # e.g., 1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms (from Sheets URL: https://docs.google.com/spreadsheets/d/{ID}/edit)
GOOGLE_CLIENT_EMAIL=your_service_account@your-project.iam.gserviceaccount.com  # From your service account JSON file
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_CONTENT_HERE\n-----END PRIVATE KEY-----\n"  # Full private key from JSON, with \n for line breaks

# Optional: Other vars (add if needed for future features)
# NEXT_PUBLIC_API_URL=http://localhost:3000  # For local API base (usually not needed)
